/**
 * Object which implements toString method.
 *
 * @since 0.1.1
 */
export interface Stringable {
	/**
	 * Converts object to a string.
	 *
	 * @return {string}
	 * @since  0.1.1
	 */
	toString(): string
}
